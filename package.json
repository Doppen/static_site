{
  "name": "Simple site builder",
  "version": "1.0.0",
  "description": "",
  "main": "buildSite.js",
  "scripts": {
    "build": "node buildSite.js",
    "server": "node app.js",
    "nodemon": "nodemon -e js,scss,html,md -x",
    "browsync": "browser-sync start --server '_dist' --files './src' ",
    "start2": "npm run build & npm run nodemon & npm run browsync",
    "start": "npm run build  & npm run nodemon  & npm run server"
  },
  "nodemonConfig": {
    "ignore": [
      "_dist/*",
      "src/components/markdown/*"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^10.0.0",
    "handlebars": "^4.7.7",
    "markdown": "^0.5.0",
    "nodemon": "^2.0.15",
    "sass": "^1.43.4"
  }
}
